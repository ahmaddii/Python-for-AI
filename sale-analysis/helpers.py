def calculate_total(quantity,price):
            """calculate the total for single item"""
            return quantity*price


def format_currency(amount):
        print(f"$ {amount:,.2f}") # 2f converts to up to 2 decimal number best for showing money

